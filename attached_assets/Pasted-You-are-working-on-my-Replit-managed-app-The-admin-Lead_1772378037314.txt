You are working on my Replit managed app. The admin “Leaderboard visibility” list has checkboxes/switches that are very buggy: clicking does not always tick/untick, sometimes it toggles the wrong row, and the UI state often disagrees with the backend.

TASK: Fix the selection/toggling so it is 100% reliable.

ROOT CAUSES TO CHECK (YOU MUST INVESTIGATE AND FIX)
1) Incorrect React keys (using array index) causing row state to stick to the wrong user.
   - Fix: ALWAYS render rows with `key={user.id}` (stable unique id).
2) Controlled vs uncontrolled inputs:
   - Fix: checkbox/switch must be controlled: `checked={user.leaderboardVisible}` (or derived from state) and only updated through state.
   - Do NOT use `defaultChecked`, and do NOT keep separate “selected” state that can drift.
3) Event handling issues:
   - Ensure the click target is consistent (row click vs checkbox click). Prevent double-firing.
   - If the whole row is clickable, make the checkbox stop propagation or remove row click and only toggle on the switch.
4) Race conditions / rapid clicks:
   - Disable the toggle while the request is in-flight per user (e.g. `pending[user.id] = true`).
   - Or debounce/throttle rapid toggles.
   - Ensure the response updates the correct user by id, not by list index.
5) State mutation bugs:
   - Do NOT mutate arrays/objects in place.
   - Use immutable updates: `setUsers(prev => prev.map(u => u.id===id ? {...u, leaderboardVisible: next} : u))`
6) Stale fetch overwriting local state:
   - If you refetch after toggles, ensure you don’t overwrite with older responses.
   - Use AbortController for fetches, or track a request version and ignore stale results.

IMPLEMENTATION REQUIREMENTS (FRONTEND)
- Single source of truth in UI state: `users: [{ id, name, role, leaderboardVisible }]`
- The switch/checkbox is controlled: `checked={user.leaderboardVisible}`
- Toggle handler must:
  a) compute `next = !user.leaderboardVisible`
  b) optimistic update local state immediately
  c) mark that user as `pending`
  d) call API `PATCH /api/leaderboard-visibility/:id` with `{ leaderboardVisible: next }`
  e) on success: replace that user with returned updated record (by id)
  f) on failure: rollback local state and show error toast
  g) finally: clear `pending`

- Disable the switch while pending: `disabled={pending[user.id]}`
- Ensure list rendering uses stable keys: `key={user.id}`

IMPLEMENTATION REQUIREMENTS (BACKEND)
- The API must update and return the updated user record including `leaderboardVisible`.
- Enforce admin auth (403 for non-admin).
- Validate user id exists; return 404 if not.

DELIVERABLES
1) Identify the exact cause(s) of buggy tick/untick in my code (keys, uncontrolled inputs, double click handlers, mutation, race conditions).
2) Provide the final updated code changes in the relevant files.
3) Confirm behavior:
   - Every click toggles exactly once
   - Correct row updates
   - No desync between UI and backend
   - Rapid clicking doesn’t break state (pending lock prevents it)

After implementing, run the app and test 20+ toggles across different users to confirm it’s stable.