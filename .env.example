DATABASE_URL=postgresql://postgres:postgres@localhost:5432/chorely

# Server-side Firebase Admin verification.
# Use literal \n in FIREBASE_PRIVATE_KEY when storing it in .env files or dashboards.
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=

# Server runtime
PORT=5000
